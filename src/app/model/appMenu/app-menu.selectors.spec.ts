import {AppMenu

.
Selectors
}
from
'./app-menu.selectors';

describe('AppMenu.Selectors', () => {
  it('should create an instance', () => {
    expect(new AppMenu.Selectors()).toBeTruthy();
  });
});
