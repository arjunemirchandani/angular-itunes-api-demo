import {Favorites

.
Selectors
}
from
'./favorites.selectors';

describe('Favorites.Selectors', () => {
  it('should create an instance', () => {
    expect(new Favorites.Selectors()).toBeTruthy();
  });
});
