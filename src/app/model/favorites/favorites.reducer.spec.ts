import {Favorites

.
Reducer
}
from
'./favorites.reducer';

describe('Favorites.Reducer', () => {
  it('should create an instance', () => {
    expect(new Favorites.Reducer()).toBeTruthy();
  });
});
