import {Favorites

.
Actions
}
from
'./favorites.actions';

describe('Favorites.Actions', () => {
  it('should create an instance', () => {
    expect(new Favorites.Actions()).toBeTruthy();
  });
});
